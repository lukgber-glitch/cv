{
  "name": "cv",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix vue-cv run dev",
    "build": "npm --prefix vue-cv run build",
    "preview": "npm --prefix vue-cv run preview"
  },
  "devDependencies": {
    "prettier": "^3.3.1",
    "vite": "^5.1.6"
  }
}
